#---------EN--------
1. read all csv files from an input directory (for example the "DataTutorial" folder) into a list of dataframes.

2. What are the dimensions/heads of the dataframes in the list?

3. read all excel sheets at once ( for example the "superstore.xls" file).
#---------SLO-----------
1. Preberi vse csv datoteke iz direktorija (npr. DataVaje) v en seznam, ki vsebuje več dataframe-ov

2. Vrni dimenzije/glave dataframeov iz seznama

3. Preberi vse excel delovne zvezke iz ene excel datoteke naenkrat (na primer iz "superstore.xls" datoteke).

